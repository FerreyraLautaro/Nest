{
  "cart": {
    "notes": `${order.customer_note}`,
    // "price_list_id": `${}`, // Esto es?
    // "operation_type_id": `${}`, // Esto es?
    // "type": `${}`, // Esto es?
    // "warehouse_id": `${}`, // Esto es?
    "date": order.date_created,
    // "user_id": `${}`,// Esto es?
    "currency_id": `${order.currency}`,// ISO?
    "general_discount": order.discount_total,
    "payment_method_id": `${order.payment_method}`,// Esto es?
    "customer": {
    //   "notes": `${order.customer_note}`,
      "vat_number": `${}`,
      "id": `${}`,
      "name": `${order.billing.first_name} ${order.billing.last_name}`
      "address": `${order.billing.address_1} (${order.billing.address_2})`,
      "state_id": `${order.billing.state}`,// Esto es?
      "city": `{order.billing.city}`,
    //   "neighborhood": `${}`,
    //   "user_id": `${}`,
    //   "price_list_id": `${}`,
      "phone": `${order.billing.phone}`,
    //   "cell_phone": `${}`,
    //   "sales_tax_group_id": `${}`,
    //   "document_id": `${}`,
      "email": `${order.billing.email}`,
      "zipcode": `${order.billing.postcode}`
    },
    "products": map(order.line_items,item => {
        return {
            "product_id": `${item.id}`,
            "size": `${}`,
            "discount": 0,
            "netPrice": item.price,
            "quantity": item.quantity
          }
    })
    ]
  }
}
